<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Estado presionado -->
    <item android:state_pressed="true" android:color="@color/accent_color"/>
    <!-- Estado seleccionado -->
    <item android:state_selected="true" android:color="@color/secondary_color"/>
    <!-- Estado por defecto -->
    <item android:color="@color/primary_color"/>
</selector>
